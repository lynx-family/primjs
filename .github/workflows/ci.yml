name: ci

on:
  push:
    branches:
      - develop
  pull_request:
    branches:
      - develop

jobs:
  coding-style:
    runs-on: lynx-ubuntu-22.04-medium
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo "to be done"
  lint-pod:
    runs-on: lynx-darwin-14-medium
    steps:
      - name: Download Source
        uses: actions/checkout@v4.2.2
      - name: Install libyaml
        run: brew install libyaml
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: '2.6'
      - name: Cache Ruby gem packages
        uses: lynx-infra/cache@main
        with:
          path: .bundle/ruby/*/cache
          key: ${{ runner.os }}-${{ runner.arch }}-gems-v1-${{ hashFiles('Gemfile', 'Gemfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-gems-v1-
      - name: Bundle Install
        run: |-
          SDKROOT=/Library/Developer/CommandLineTools/SDKs/MacOSX.sdk bundle install --path .bundle
      - name: Lint
        run: |-
          POD_VERSION=0.0.1-for-ci-test PRIMJS_COMMIT_ID=${{ github.event.pull_request.head.sha }} bundle exec pod spec lint PrimJS.podspec --verbose --skip-import-validation --allow-warnings
  
  check-unittests-linux:
    runs-on: lynx-ubuntu-22.04-medium
    steps:
      - name: Download Source
        uses: actions/checkout@v4.2.2
      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: "3.9"
      - name: Cache Habitat dependencies
        uses: lynx-infra/cache@main
        with:
          path: |
            ~/.habitat_cache/habitat
            ~/.habitat_cache/git
            ~/.habitat_cache/objects
          key: ${{ runner.os }}-${{ runner.arch }}-habitat-v1-${{ hashFiles('.habitat', 'DEPS', 'tools/hab') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-habitat-v1-
      - name: Install deps
        run: |
          source tools/envsetup.sh
          hab sync . -f
      - name: Build unittests
        run: |
          source tools/envsetup.sh
          python3 tools/ci/check_test_build.py
      - name: Run unittests
        run: 
          python3 tools/ci/check_test_run.py

  check-unittests-darwin:
    runs-on: lynx-darwin-14-medium
    steps:
      - name: Download Source
        uses: actions/checkout@v4.2.2
      - name: Cache Habitat dependencies
        uses: lynx-infra/cache@main
        with:
          path: |
            ~/.habitat_cache/habitat
            ~/.habitat_cache/git
            ~/.habitat_cache/objects
          key: ${{ runner.os }}-${{ runner.arch }}-habitat-v1-${{ hashFiles('.habitat', 'DEPS', 'tools/hab') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-habitat-v1-
      - name: Install deps
        run: |
          source tools/envsetup.sh
          hab sync . -f
      - name: Build unittests
        run: |
          source tools/envsetup.sh
          python3 tools/ci/check_test_build.py
      - name: Run unittests
        run: 
          python3 tools/ci/check_test_run.py
      - name: Run wasm tests
        run: |
          source tools/envsetup.sh
          bash testing/wasm/wasm_run.sh
